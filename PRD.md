# travel-salary-hub 旅行行业薪酬看板 · PRD

版本 v0.3 · 2026-09-17 · 仓库 `github.com/tuobadaidai/travel-salary-hub`（私有）

## 1. 背景

DIDA（道旅）是跨境旅行 B2B 平台。HR 团队在定薪、调薪、招聘定价时缺少行业参照：

- 竞争公司（OTA + 旅行 B2B 平台）的薪酬数据散落在招聘聚合站、上市公司财报、搜索摘要里，口径不一
- 内部薪酬（704 人）与市场数据没有可比对工具，对标靠人工查表
- 海外驻地（销售岗为主）薪酬无任何内部参照

## 2. 产品目标与验收标准

一句话：**在面板上查询同一岗位在不同级别、不同区域的薪酬情况，支撑薪酬对标、薪酬水平评估、薪酬策略制定。**

- 看板可按 岗位 × 级别 × 区域 三维查询市场薪酬分位数
- 同一视角对照 DIDA 内部薪酬与差距百分比
- 每个数字可穿透到原始记录（证据链）
- 样本不足的数据明确标记，不误导决策

## 3. 用户与使用场景

| 用户 | 场景 | 频次 |
|---|---|---|
| DIDA HR（当前单人使用） | 调薪季批量对标核心岗位 | 月/季 |
| 同上 | 招聘定价：offer 前查某岗位某城市市场区间 | 周 |
| 同上 | 薪酬策略：级别 × 区域差距热力分析 | 季 |

单机内部工具，无多租户。当前未接入鉴权，仅限本机访问（127.0.0.1:8300）。

## 4. 核心概念与数据口径

- **市场级别** `level`：专员 / 高级 / 主管 / 经理 / 总监 / VP（由岗位名推断）
- **DIDA 职级** `dida_grade`：P0-P8（专业）/ O1-O4（运营）/ M4-M5（管理），有等效映射到 level（`import_dida_payroll.py` GRADE_TO_LEVEL）
- **岗位归一** `position_norm`：市场岗位名归一后入库，对标按归一名匹配
- **薪酬口径**：统一折算 CNY/年（`annual_salary_avg_base`），20 币种汇率
- **可靠性**：组内样本 <5 标记 `reliable=false`，前端置灰
- **两套口径分列不混算**：财报人均薪酬（含社保/股权摊销）vs JD/聚合现金薪酬

## 5. 功能需求

### F1 总览 Dashboard（已上线）

- 岗位族年薪分位数区间图（P25-P75 区间带 + P90 + P50 菱形标记，样本<5 置灰）
- 公司类型对比图
- 任意两维交叉透视热力图（行/列维度可切：城市/国家/公司类型/岗位族/级别/岗位）
- 点击格子穿透原始记录抽屉（来源/区间/年薪/原文链接）
- 全局筛选：城市多选 / 国家 / 公司类型 / 岗位族

### F2 岗位对标 Benchmark（已上线，核心页面）

- 选择岗位 → 级别 × 城市 矩阵；每城市三列：市场 P50 / DIDA 内部 P50 / 差距百分比
- 差距 ≥±10% 高亮（高于市场红 / 低于市场绿）
- 市场格穿透市场明细记录；DIDA 格穿透内部摘要
- 岗位下拉按记录数排序 + 关键字过滤
- 验收：任一 DIDA 核心岗位（客服专员/后端工程师/产品经理等）可出矩阵

### F3 公司名单与财报对照（已上线）

- 19 家竞争公司详情（A 级上市 7 / B 级非上市 6 / C 级子公司 2 / D 级新兴 4）
- 财报人均薪酬对照（独立口径展示）

### F4 数据采集引擎（已上线，命令行驱动）

- 三通道：SearxNG(3004) 搜索发现 → scrapling（chrome 指纹）主抓 → Crawl4AI(11235) 备用
- 国内批量 `run_batch`：29 个 DIDA 核心岗位 × 8 城市；`--senior` 追加 14 个高级/主管岗位独立聚合页
- 竞争公司子页 `run_companies`：6 家已验证 ID（携程/美团/同程/众信/马蜂窝/阿里）
- 海外 `run_overseas`：12 城市 × 5 销售岗，搜索摘要抽取，`confidence=low`
- DIDA 内部导入 `import_dida_payroll`：薪酬表 xlsx 匿名化导入，pay_hash 幂等
- 合规约束：4s 限速、robots 检查、字段白名单、原始页面 JSONL 落盘审计

### F5 采集管理（部分实现）

- collect_runs 批次记录（来源/状态/新增数/参数），`/admin/collect-runs` 只读
- 调度器未实现（`ingest_schedules` 表已预留）

## 6. 数据需求

| 数据源 | 表 | 规模（2026-09-17） | 口径/置信 |
|---|---|---|---|
| jobui 城市聚合页 | salary_records | 295 | 聚合分布，较高 |
| jobui 公司子页 | salary_records | 137 | 区间近似，中 |
| 海外搜索摘要 | salary_records | 34 | low，20 币种 |
| 历史 JD（前程无忧/智联/猎聘） | salary_records | 66 | 单条 JD |
| DIDA 薪酬表（xlsx） | dida_payroll | 704 | 内部真实，匿名化 |
| 财报/统计局 | reports | seed | 人均薪酬（独立口径） |

存储：SQLite（WAL）`data/salary.db`；敏感数据（db/raw/seed）不入 git。

## 7. 非功能需求

- 部署：单机，FastAPI 托管前端 dist，一条 uvicorn 命令启动
- 安全：当前仅绑定 127.0.0.1；**多人使用前必须加鉴权**（薪酬数据敏感度高）
- 审计：采集原始页面 JSONL 存档 `data/raw/`
- 备份：`scripts/backup.sh`

## 8. 已知问题（v0.3 review 结论，按影响排序）

数据正确性（直接影响面板数字，修复优先）：

1. 海外 snippet 混口径：年薪被当月薪（例：香港业务经理 103 万/年）；`$` 城市归属错判（美国/加拿大岗位混入曼谷）
2. 公司子页取区间中点，未用分布加权 → 系统性虚高（例：20-50K → 42 万/年）
3. 对标岗位匹配 LIKE 过宽：「产品经理」混入「资深产品经理」
4. 海外中位数聚合混币种、混月薪/年薪口径
5. 聚合页 sample_count 存岗位全量样本，非级别内样本，可靠性标记失真

代码缺陷（中低危）：

6. SPA fallback 路径穿越（`GET //data/salary.db` 可下载数据库；因仅本机绑定，暴露面有限）
7. `infer_level` 正则误判（coordinator→VP；「总部X」→总监）
8. 热力图穿透对岗位族/公司类型维度过滤不完整，抽屉显示无关记录
9. ECharts 实例与 resize 监听器泄漏（反复筛选后页面变慢）
10. `fx_rates` 表已建未接线，折算用硬编码快照
11. dida `pay_hash` 不含 currency/grade，改薪后重导入产生重复行

## 9. 后续规划

- **P0**：修复第 8 节数据正确性问题并重跑采集清洗（当前数字可能误导薪酬决策）
- **P1**：鉴权 + 内网部署供 HR 同事使用；前端 API 地址改相对路径；月度调度器
- **P2**：Glassdoor 结构化数据（登录墙方案）；VP 档市场数据；途牛/飞猪独立页；内部对标差距页深化（序列/职级切片）

**范围外**：多租户、权限分级、payroll 精确到人、对外发布。
