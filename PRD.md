# travel-salary-hub 旅行行业薪酬看板 · PRD

版本 v0.11 · 2026-09-22 · 仓库 `github.com/tuobadaidai/travel-salary-hub`（私有）

## 1. 背景

DIDA（道旅）是跨境旅行 B2B 平台。HR 团队在定薪、调薪、招聘定价时缺少行业参照：

- 竞争公司（OTA + 旅行 B2B 平台）的薪酬数据散落在招聘聚合站、上市公司财报、搜索摘要里，口径不一
- 内部薪酬（704 人）与市场数据没有可比对工具，对标靠人工查表
- 海外驻地（销售岗为主）薪酬无任何内部参照

## 2. 产品目标与验收标准

一句话：**在面板上查询同一岗位在不同级别、不同区域的薪酬情况，支撑薪酬对标、薪酬水平评估、薪酬策略制定。**

- 看板可按 岗位 × DIDA 职级 × 区域 三维查询薪酬分位数
- 同一视角对照 DIDA 内部薪酬与市场参照、差距百分比
- 每个数字可穿透到原始记录（证据链）
- 样本不足的数据明确标记，不误导决策

### 2.1 量化验收（v0.11 更新）

| 指标 | 现状（9-22 实测） | 目标 |
|---|---|---|
| DIDA 侧 grade×城市格样本≥5 | 28/153 (18%) | ≥40%（含"全国"聚合格） |
| 市场侧参照格（pos×level×城市）样本≥5 | 1/426 (0.2%) | 不强求密集，改为「级别带参照 + 全国基准」双层 |
| 对标岗位数（两端都有数据） | 待测 | ≥20 个核心岗位 |
| 数字可解释 | 差距无口径说明 | 每格标注样本量/口径/置信 |

## 3. 用户与使用场景

| 用户 | 场景 | 频次 |
|---|---|---|
| DIDA HR（当前单人使用） | 调薪季批量对标核心岗位 | 月/季 |
| 同上 | 招聘定价：offer 前查某岗位某城市市场区间 | 周 |
| 同上 | 薪酬策略：级别 × 区域差距热力分析 | 季 |

单机内部工具，无多租户。当前未接入鉴权，仅限本机访问（127.0.0.1:8300）。

## 4. 核心架构决策（v0.11 重设计）

### AD-1 对标主轴 = DIDA 职级，市场为参照（不是镜像）

**问题**：市场侧 salary_records 仅 521 条，切到「岗位 × 级别 × 城市」三维后 426 格中只有 1 格样本≥5（99.8% 置灰）。此前把 15 个 DIDA 职级映射到 6 档市场 level 再查市场数据，映射本身造成信息坍缩（P0/P1 都映射"专员"），且市场格依然几乎全空。

**决策**：对标矩阵以 **DIDA 内部数据为主轴**（grade × 城市，P 序列 550 条、样本集中），市场数据分层作为参照：
1. **级别带参照**（放宽到「岗位 × level」全国聚合，135 格中 41 格 ≥5）：回答"这个岗位的市场价大概在什么带宽"
2. **全国基准**（Michael Page 等薪酬报告 report: 前缀，按级别聚合）：权威但粒度粗
3. 城市 × 级别交叉格只在样本足够时展示，否则降级显示上一层的数字并标注

### AD-2 内部数据先清洗再对标

- dida_payroll 704 条中 12 条 grade 为空、55 条 seq 为空 → 导入时按 title 关键词回填 grade 规则
- 岗位匹配用「归一化岗位族 + title 关键词」两级：先归一化（资深/高级/技术/后台等修饰词剥离）到岗位族，再在族内按 grade 对标——市场侧同理收敛，避免「高级客服」与「客服」被当成两个岗

### AD-3 海外数据单独舱，不混入国内对标

现状 23 条海外记录 5 条 >100 万（雅加达 IDR 24.7 亿=汇率折算错误级别），混入会污染分位数。决策：
- 海外数据强制 `country != 'CN'` 隔离，国内对标默认排除
- 独立「海外驻地」视图：按 国家 × 岗位 展示，置信度固定 low，仅用于方向性参考
- 修复 IDR 等汇率折算（接通 fx_rates 表，删除硬编码 DEFAULT_FX）

### AD-4 数据正确性修复优先于新数据源

猎聘/前程无忧部分记录 months=15/18/20 属异常（猎聘法务总监均值 77 万 vs 中位 58 万，8 条>100 万拉偏分布）。决策：
- 月份>13 的记录标 `confidence=low`，分位数用**中位数**（对离群值稳健），前端 P50 为主展示
- PRD §8 v0.3 列的 1-5 号数据正确性问题已在 v0.4-v0.6 修复；剩余 10/11（fx_rates 接线、pay_hash 口径）在本版完成

### AD-5 稀疏数据不硬凑矩阵，用「三层降级」呈现

对标页每格展示优先级：
1. 本岗位 × 本 grade × 本城市（DIDA 内部样本≥5 才显示分位带）
2. 本岗位 × 本 grade 全国（DIDA 样本≥5）
3. 本岗位 × 等效级别带市场参照（全国）
每格标注当前所在层，杜绝"看起来准其实只有 1 条"的误导。

## 5. 功能需求

### F1 总览 Dashboard（已上线）

- 岗位族年薪分位数区间图（P25-P75 区间带 + P90 + P50 菱形标记，样本<5 置灰）
- 公司类型对比图；交叉透视热力图 + 点击穿透；全局筛选条

### F2 岗位对标 Benchmark（本版重构，核心页面）

- 选择岗位 → **DIDA 职级行（P0-P8/O1-O4/M4-M5，按序列过滤）× 城市列**矩阵
- 每格：DIDA P50（内部真实）+ 级别带市场参照 + 差距%，按 AD-5 三层降级
- 全国基准行（Michael Page 报告）单独一行展示，不参与 gap 计算
- 点格穿透：DIDA 格→内部明细（匿名化），市场格→原始记录
- 验收：产品经理/后端工程师/客服专员/销售经理 4 个岗位出完整可读矩阵

### F3 公司名单与财报人效对照（已上线）

- 19+1 家竞争公司，年报人均人力成本/人均营收/员工数（A股 high / 港股 medium）

### F4 数据采集引擎（已上线）

- 三通道（SearxNG→scrapling→Crawl4AI）+ 公司子页 + 海外摘要 + 年报 PDF + Michael Page PDF + Levels.fyi(Apify) + 51job 骨架
- 合规约束：4s 限速、robots 检查、字段白名单、JSONL 审计落盘

### F5 采集管理与调度（v0.10 已上线）

- collect_runs 批次记录；在线触发；后台线程调度器（jobui_companies 每 7 天）
- **本版补**：调度完成回调写 last_success_at（当前只写 last_run_at，导致到期必重跑）

### F6 海外驻地视图（本版新增）

- 国家 × 岗位 的 DIDA 内部薪酬（印度尼西亚 18 / 马来西亚 11 …）+ 海外市场摘要（隔离展示，low 置信）
- 币种原值 + CNY 折算双列

## 6. 数据需求

| 数据源 | 表 | 规模（9-22） | 口径/置信 |
|---|---|---|---|
| jobui 城市聚合页 | salary_records | 295 | 聚合分布，较高 |
| jobui 公司子页 | salary_records | 137 | 分布加权，中 |
| 海外搜索摘要 | salary_records | 23 | low，隔离舱 |
| 历史 JD（前程无忧/智联/猎聘） | salary_records | 66 | 单条 JD；months>13 置 low |
| Michael Page 报告 | salary_records | 待导入 | report: 前缀，全国基准 |
| DIDA 薪酬表（xlsx） | dida_payroll | 704 | 内部真实，匿名化 |
| 财报/统计局 | reports | 8 | 人均薪酬（独立口径） |

存储：SQLite（WAL）`data/salary.db`；敏感数据（db/raw/seed）不入 git。

## 7. 非功能需求

- 部署：单机，FastAPI 托管前端 dist，一条 uvicorn 命令启动
- 安全：仅绑定 127.0.0.1；**多人使用前必须加鉴权**（薪酬数据敏感度高）
- 审计：采集原始页面 JSONL 存档 `data/raw/`；备份 `scripts/backup.sh`

## 8. 已知问题（v0.11 重新盘点）

已在 v0.4-v0.6 修复：原 1-5（海外混口径/中点虚高/LIKE 过宽/混币种/sample_count 语义）、6（路径穿越）、7（infer_level）、8（穿透参数）、9（ECharts 泄漏）。

本版待修（P0）：
1. **fx_rates 表已建未接线**，折算用硬编码 DEFAULT_FX → 雅加达 IDR 出现 24.7 亿年薪级错误
2. **dida pay_hash 不含 currency/grade**，改薪重导入产生重复行
3. **对标矩阵市场格 99.8% 空** → 按 AD-1/AD-5 重构呈现层
4. **海外数据未与国内隔离**，混入对标分位数
5. 调度器只写 last_run_at，每周期必重跑

本版不做（P1+）：鉴权、API 相对路径、VP 档市场数据、Glassdoor 登录墙。

## 9. 后续规划

- P1：鉴权 + 内网部署；月度调度补全（levels_fyi/51job 接入）
- P2：薪酬带宽建议（P25-P75 区间推荐 offer）；年度调薪矩阵导出

**范围外**：多租户、权限分级、payroll 精确到人、对外发布。
